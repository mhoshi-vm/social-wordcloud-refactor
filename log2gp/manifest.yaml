---
applications:
- name: log2gp
  memory: 512M
  buildpacks:
    - ruby_buildpack
    - binary_buildpack
  command: bundle exec fluentd -c ./fluent.conf
  env:
    BUNDLE_WITHOUT: development:test
  health-check-type: process
  routes:
    - route: tcp.app.tas.vpsmart.aws.lespaulstudioplus.info:5140
  services:
    - greenplum