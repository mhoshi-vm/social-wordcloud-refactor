CREATE ALIAS IF NOT EXISTS vader_sentiment AS $$
double vaderSentiment(String text) {
    // This is a Java stub to replace the Python logic.
    // H2 runs in the JVM, so we use Java here.
    return 0.0;
}
$$;